void setup() {
  // put your setup code here, to run once:
  
  // Pins can be in INPUT or OUTPUT mode but only one at a time
  // First input is the Pin number
  // Second input to the function is they mode: INPUT, INPUT_PULLUP, or OUTPUT
  //pinMode(2, INPUT);
  pinMode(2, INPUT_PULLUP);
  //pinMode(2, OUTPUT);
}

void loop() {
  // put your main code here, to run repeatedly:

}
